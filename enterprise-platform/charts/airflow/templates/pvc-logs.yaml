{{- if .Values.storage.logs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "airflow.fullname" . }}-logs
  labels:
    {{- include "airflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    {{- range .Values.storage.logs.accessModes }}
    - {{ . | quote }}
    {{- end }}
  resources:
    requests:
      storage: {{ .Values.storage.logs.size | quote }}
  {{- if .Values.storage.logs.storageClass }}
  {{- if (eq "-" .Values.storage.logs.storageClass) }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.storage.logs.storageClass | quote }}
  {{- end }}
  {{- else if .Values.global }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass | quote }}
  {{- end }}
  {{- end }}
{{- end }}